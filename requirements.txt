# SACT Intelligent Scheduling System
# Python 3.10+ required

# =============================================================================
# Core Framework
# =============================================================================
# Security pins (T4.6): upper bound unpinned so patch releases flow through,
# lower bound set to the first release that picked up known-CVE fixes.
streamlit>=1.39.0        # CVE patches for websocket handling
pandas>=2.2.0
numpy>=1.26.4            # CVE-2024-8090 fix (object-dtype buffer overflow)

# =============================================================================
# Excel Processing
# =============================================================================
openpyxl==3.1.2
xlsxwriter==3.1.9

# =============================================================================
# Optimization
# =============================================================================
ortools==9.8.3296

# =============================================================================
# Machine Learning
# =============================================================================
scikit-learn==1.4.0
xgboost==2.0.3
shap==0.44.0
joblib==1.3.2

# =============================================================================
# NLP & Sentiment Analysis
# =============================================================================
nltk==3.8.1
vaderSentiment==3.3.2
spacy==3.7.2
# After install, run: python -m spacy download en_core_web_sm

# =============================================================================
# External Data & APIs
# =============================================================================
requests>=2.32.0         # CVE-2024-35195 fix (.netrc credential leak)
urllib3>=2.0.0           # TLS verification hardening (transitively via requests)
feedparser>=6.0.10
geopy>=2.4.1

# =============================================================================
# Visualization
# =============================================================================
plotly==5.18.0
folium==0.15.1
streamlit-folium==0.17.0

# =============================================================================
# Web framework + production extensions (T4.1 auth, T4.2 rate limits,
# T4.3 CSRF, T4.7 secrets management)
# =============================================================================
Flask>=3.0.0
Flask-Login>=0.6.3
Flask-WTF>=1.2.1
Flask-Limiter>=3.5.0
python-dotenv>=1.0.0

# Observability (T4.5) — Prometheus metrics + OpenTelemetry tracing.
# OTel extras are OPTIONAL; uncomment only when you run an OTLP collector.
prometheus_client>=0.20.0
# opentelemetry-api>=1.25.0
# opentelemetry-sdk>=1.25.0
# opentelemetry-exporter-otlp-proto-http>=1.25.0

# =============================================================================
# Utilities
# =============================================================================
python-dateutil==2.8.2
pytz==2024.1
